define Start(parameter)
{

	y <- 0
	x <- Sin(342)
	local <- 44
	#Tests multi-line arguments:
	gGlobal <- 999 +
                   33  + 252 - 
                   3252;  Print("gGlobal after multi-line: " + gGlobal)
	Sub1(333)
	Sub2(33);
	Print(gGlobal + " Should be 33")
	Print(local + " Should be 44")
}


define Sub1(par)
{	
	Print("Executing Sub1: "+ par )
	gGlobal <- 33
}

define Sub2(ou)
{
	Print("Executing Sub2")
	local  <- 99339
}